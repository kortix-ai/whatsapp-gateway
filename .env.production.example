DOMAIN=whatsapp.example.com
GATEWAY_IMAGE=ghcr.io/kortix-ai/whatsapp-gateway:main
GATEWAY_RELEASE=main
# Exact CDN/reverse-proxy CIDRs; leave empty for direct-origin deployments.
TRUSTED_PROXY_CIDRS=
POSTGRES_DB=whatsapp_gateway
POSTGRES_USER=whatsapp_gateway
POSTGRES_PASSWORD=replace-with-a-long-random-password
DATABASE_URL=postgresql://whatsapp_gateway:replace-with-a-long-random-password@postgres:5432/whatsapp_gateway
BETTER_AUTH_SECRET=replace-with-at-least-32-random-characters
ENCRYPTION_KEY=replace-with-32-byte-base64-key
AUTH_ALLOWLIST_ENABLED=true
ALLOWED_EMAILS=marko@kortix.ai
WORKER_CAPACITY=25
RECONNECT_STABLE_SECONDS=300
# Import full device history on pairing (heavier, slower pairing; without it only recent history arrives).
SYNC_FULL_HISTORY=true
# Residential proxy for the WhatsApp socket (avoids datacenter-IP flagging). http(s):// or socks5://.
WA_PROXY_URL=
PAIRING_TTL_SECONDS=300
WEBHOOK_MAX_ATTEMPTS=12
WEBHOOK_CONCURRENCY=10
